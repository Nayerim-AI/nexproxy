import{api}from'./api';import{certificates,dns,routes,scenario}from'./mock';
import{deriveServices}from'./model';
export interface DataSource{routes:typeof api.routes;dns:typeof api.dns;certificates:typeof api.certificates;services:typeof api.services;activity:typeof api.activity;settings:typeof api.settings;system:typeof api.system;create:typeof api.create;update:typeof api.update;remove:typeof api.remove;recheck:typeof api.recheck;saveSettings:typeof api.saveSettings;testTraefik:typeof api.testTraefik}
const production:DataSource=api;const mock:DataSource={routes:async()=>routes,dns:async()=>({status:'available',provider:'mock',tokenConfigured:true,records:dns}),certificates:async()=>certificates,services:async()=>({desired:deriveServices(routes),observed:[],stale:false}),activity:async()=>[],settings:async()=>({tokenConfigured:true}),system:async()=>({api:'healthy',database:'healthy',runtime:'connected',message:'Local mock API responding'}),create:async(v)=>({result:'success',resource:{...routes[0],...v,id:`r${Date.now()}`},steps:[],message:'route applied'}),update:async(id,v)=>({result:'success',resource:{...routes.find(r=>r.id===id)!,...v},steps:[],message:'route updated'}),remove:async()=>({result:'success',steps:[],message:'route deleted'}),recheck:async(id)=>({result:'success',resource:routes.find(r=>r.id===id),steps:[],message:'route checked'}),saveSettings:async v=>({tokenConfigured:true,...v}),testTraefik:async()=>({status:'healthy',version:'mock',stale:false,message:'Mock test passed'})};
/** Explicit development scenarios are mocks. Normal mode always uses HTTP and never falls back. */
export const dataSource=():DataSource=>scenario()==='normal'?production:mock;
